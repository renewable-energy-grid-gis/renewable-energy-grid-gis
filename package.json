{
  "name": "renewable-energy-grid-gis-org",
  "version": "1.0.0",
  "private": true,
  "description": "Python for Renewable Energy & Grid GIS Automation — production-focused resource for spatial workflows, compliance automation, and scalable geospatial pipelines.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site",
    "wrangler:dev": "wrangler dev",
    "deploy": "npm run clean && npm run build && wrangler deploy",
    "deploy:dry-run": "npm run build && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "katex": "^0.16.9",
    "markdown-it": "^14.0.0",
    "markdown-it-anchor": "^8.6.7",
    "markdown-it-attrs": "^4.1.6",
    "markdown-it-footnote": "^4.0.0",
    "markdown-it-task-lists": "^2.1.1",
    "mermaid": "^11.15.0",
    "prismjs": "^1.29.0",
    "slugify": "^1.6.6"
  }
}
